<% if flash[:notice] %>
  <div class="flash-message">
    <%= flash[:notice] %>
  </div>
<% end %>

<h1>Plantas</h1>

<%= link_to "Añadir planta", new_plant_path %>

<h2>Biología</h2>

<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th>Nombre</th>
      <th>Familia</th>
      <th>Luz</th>
      <th>Humedad</th>
      <th>Profundidad de raíz</th>
      <th>Diámetro de raíz (m)</th>
      <th>Tª máxima (ºC)</th>
      <th>Tª mínina (ºC)</th>
      <th>Tª germinación (ºC)</th>
    </tr>
  </thead>
  <tbody>
    <% @plants.each do |data| %>
      <tr>
        <td>
          <%= link_to "Editar", edit_plant_path(data) %>
          <%= link_to "Borrar", plant_path(data), data: {
                    turbo_method: :delete,
                    turbo_confirm: "¿Quieres eliminar esta planta?"
                  } %>
        </td>
        <td>. . .</td>
        <td><%= link_to data[:name], plant_path(data[:id]) %></td>
        <td><%= data[:family] %></td>
        <td><%= data[:light] %></td>
        <td><%= data[:humidity] %></td>
        <td><%= data[:root_depth] %></td>
        <td><%= data[:root_diameter] %></td>
        <td><%= data[:maximum_temperature] %></td>
        <td><%= data[:minimum_temperature] %></td>
        <td><%= data[:germination_temperature] %></td>
      </tr>
    <% end %>
  </tbody>
</table>
