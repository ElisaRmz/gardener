<h1> Añade una nueva tierra </h1>

<%= form_for @land do |f| %>
  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit "Guardar" %>
  </div>
<% end %>